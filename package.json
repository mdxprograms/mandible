{
  "name": "mandible",
  "version": "0.0.1",
  "description": "Example components and structure using mantle",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "parcel src/index.html -p 3000",
    "build": "rm -rf dist && parcel build src/index.html --detailed-report --no-source-maps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@wallerbuilt/mantle": "^1.0.6"
  },
  "devDependencies": {
    "eslint": "^7.14.0",
    "parcel-bundler": "^1.12.4",
    "prettier": "^2.2.1",
    "typescript": "^4.1.2"
  }
}
